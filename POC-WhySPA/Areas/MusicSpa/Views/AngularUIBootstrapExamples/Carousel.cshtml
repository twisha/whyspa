<carousel interval="myInterval">
    <slide data-ng-repeat="slide in slides">
        <img data-ng-src="{{slide.image}}" style="margin:auto;">
        <div class="carousel-caption">
            <h4>Slide {{$index}}</h4>
            <p>{{slide.text}}</p>
        </div>
    </slide>
</carousel>