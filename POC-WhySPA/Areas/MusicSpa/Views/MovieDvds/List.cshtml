@using POC_WhySPA.Areas.MusicSpa.Resources.MovieDvds
<h3>Movie DVDs</h3>
<form class="form-inline" role="form" data-ng-submit="search()">
    <div class="form-group">
        <label for="selectGenres">@MovieDvds.LabelGenres</label>
        <select id="selectGenres" data-ng-model="selectedGenre" class="form-control">
            <option data-ng-selected="{{genre.Key == selectedGenre}}"
                data-ng-repeat="genre in genres" value="{{genre.Key}}">{{genre.Value}}</option>
        </select>
    </div>
    <button type="submit" class="btn btn-primary">Search</button>
</form>
<table class="table" data-ng-table="tableParams" data-show-filter="true" data-template-pagination="custom/pager">
    <tr data-ng-repeat="movieDvd in $data">
        <td data-title="'#'">
            <button data-ng-click="view(movieDvd.Id)" class="btn btn-default btn-xs">
                <span class="glyphicon glyphicon-eye-open"></span>
            </button>
            <a href="#/Edit/{{movieDvd.Id}}" class="btn btn-default btn-xs">
                <span class="glyphicon glyphicon-edit"></span>
            </a></td>
        <td data-title="'@MovieDvds.HeaderTitle'" data-sortable="'Title'" data-filter="{ 'Title': 'text' }">{{movieDvd.Title}}</td>
        <td data-title="'@MovieDvds.HeaderPrice'" data-sortable="'Price'">{{movieDvd.Price}}</td>
        <td data-title="'@MovieDvds.HeaderGenre'" data-sortable="'Genre'">{{movieDvd.Genre}}</td>
    </tr>
</table>
@Html.Partial("Pager")